(function(d3, fc, sc) {
    'use strict';

    sc.model.xAxis = function(initialPeriod) {
        return {
            viewDomain: [],
            period: initialPeriod
        };
    };

})(d3, fc, sc);
